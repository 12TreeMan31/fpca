fn ccl_uf(px: Vec<u8>, width: usize, height: usize) {
    const SIZE: usize = width * height;
    const dy: [usize; 4] = [-width, -width, -width, 0];
    const dx: [usize; 4] = [-1, 0, 1, -1];

    // Create Node
    // Create Vector of nodes
}
